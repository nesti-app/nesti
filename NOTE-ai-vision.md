# Нотатка: «AI-заповнення за фото» для Nesti

Статус: **оцінка, не впроваджується** (рішення користувача від 2026-09-18).

## Суть фічі
Користувач завантажує фото предмета → AI-модель зору аналізує зображення і заповнює поля форми: опис, виробник, модель, серійний номер, SKU, ціна/валюта, теги + атрибути. Користувач підтверджує поля перед збереженням.

### Вибраний дизайн (рішення користувача)
- **Коли запускати:** кнопка «Заповнити за фото» у формі (не авто після завантаження).
- **Що заповнювати:** все — опис + метадані + теги + атрибути, з підтвердженням перед збереженням.
- **Провайдер:** поки не обрано — фічу не реалізуємо, лише фіксуємо оцінку.
- На вхід моделі йде вже готовий WebP (стиснений до 2400px у `app/media/service.py:130`), ~300–800 токенів.

## Експлуатаційні витрати (тарифи 2026)

| Провайдер | Модель vision | ~за 1 фото | 100 фото/міс | 500 фото/міс |
|---|---|---|---|---|
| OpenAI | gpt-4o-mini | $0.001–0.003 | $0.1–0.3 | $0.5–1.5 |
| Google | gemini-2.0-flash | ~$0.001 | ≈$0 (free-tier) | ~$0.5 |
| Anthropic | claude-haiku | $0.002–0.004 | $0.2–0.4 | $1–2 |
| Локально | Ollama Qwen-VL/LLaVA | $0 | $0 | $0 (потрібне GPU; на CPU ~5–15 с/фото) |

Висновок: для домашнього інвентаря — копійки або $0 (Gemini free-tier / локальна модель).

## Що вже є в проєкті (готовність)
- Фото нормалізується → WebP у S3; URL через `GET /items/{id}/images/{image_id}/file` (`app/media/routes.py:107`).
- Модель `Item` (`app/items/models.py:25`) має всі цільові поля; атрибути — key-value таблиця `item_attributes`.
- Інтернет-доступ з контейнера відкритий (немає egress-обмежень у `compose.yaml`), але це буде перший outbound-виклик у коді.
- Конфіг через `.env` + `pydantic-settings` (`app/config.py`) — ключі AI додаються за патерном `s3_*`.

## Зміни, які знадобляться (обсяг робіт)
1. `app/ai/` — новий модуль: клієнт провайдера + промпт «image → JSON» + Pydantic-модель відповіді.
2. Роут `POST /items/{item_id}/ai-suggest` (або prefill) у `app/items/routes.py`.
3. UI в `templates/items/form.html`: кнопка «Заповнити за фото» (Alpine/htmx), заповнення полів із підтвердженням.
4. `app/config.py`: налаштування провайдера (ключі, `ai_enabled`).
5. Опційно таблиця `item_ai_analysis` (JSON-кеш, щоб не переплачувати за повторний аналіз) + міграція.
6. Залежності в `pyproject.toml` → `uv.lock` у Docker build.
7. Тести + гейти (pytest/ruff/mypy).

**Оцінка часу:** ~3–6 год на хмарний варіант; локальний (Ollama) +2–4 год на додатковий сервіс у compose та налаштування моделі.

## Ризики/нюанси
- Безкоштовний старт: **Gemini 2.0 Flash** має generous free-tier — оптимально для тесту.
- Серійник/SKU можуть читатись неточно → тому дизайн «юзер підтверджує поля перед збереженням».
- Конфіденційність: хмара = фото покидають сервер; локальна модель це знімає.
- Схема БД не потребує міграцій для основних полів (лягають у наявні); `item_ai_analysis` — опційно.